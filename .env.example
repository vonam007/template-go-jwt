PORT=8080
JWT_SECRET=dev-secret-change-me
JWT_TTL_SECONDS=3600

# Postgres settings (used by docker-compose and the app when running in compose)
DB_HOST=db
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=template
